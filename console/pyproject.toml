[project]
name = "console"
version = "0.1.0"
description = "Console điều hành hợp nhất cho các công ty AI của X-Agents"
requires-python = ">=3.11"
# Không có phụ thuộc runtime ngoài hai công ty: server dùng thư viện chuẩn (http.server, sqlite3).
dependencies = [
    "software-company",
    "video-creators",
]

[tool.uv.sources]
software-company = { path = "../software-company", editable = true }
video-creators = { path = "../Studio-creators", editable = true }

[dependency-groups]
dev = [
    "mypy>=1.10",
    "pytest>=8",
    "pytest-cov>=5",
    "ruff>=0.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/console"]

[tool.ruff]
line-length = 120
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B", "UP", "RUF"]
# E701/E702: cùng quy ước viết gọn một dòng như hai công ty. RUF001-003: ký tự Unicode tiếng Việt.
ignore = ["E701", "E702", "RUF001", "RUF002", "RUF003"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.coverage.run]
source = ["src/console"]

[tool.coverage.report]
# Đặt ở mức đang đạt để chặn tụt lùi, nâng lên khi coverage thật tăng.
fail_under = 85
