name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  static:
    name: static
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: uv sync
        working-directory: software-company
      - run: uv run ruff check src tests
        working-directory: software-company

  unit:
    name: unit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: uv sync
        working-directory: software-company
      - run: uv run pytest -q
        working-directory: software-company

  eval-replay:
    name: eval-replay
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: uv sync
        working-directory: software-company
      # Eval từ bản ghi (ADR-0010): không gọi model. Agent chưa có bản ghi → SKIP; bản ghi lệch prompt → đỏ.
      - run: PYTHONPATH=src uv run python -m company.evals all --replay
        working-directory: software-company

  studio-static:
    name: studio-static
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: uv sync
        working-directory: Studio-creators
      - run: uv run ruff check src tests
        working-directory: Studio-creators

  studio-unit:
    name: studio-unit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: sudo apt-get update && sudo apt-get install -y ffmpeg
      - run: uv sync
        working-directory: Studio-creators
      - run: uv run pytest -q
        working-directory: Studio-creators

  studio-eval-replay:
    name: studio-eval-replay
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: uv sync
        working-directory: Studio-creators
      - run: PYTHONPATH=src uv run python -m studio.evals all --replay
        working-directory: Studio-creators

  gateway-static:
    name: gateway-static
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: uv sync
        working-directory: gateway
      - run: uv run ruff check src tests
        working-directory: gateway

  gateway-unit:
    name: gateway-unit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: uv sync
        working-directory: gateway
      - run: uv run pytest -q
        working-directory: gateway

  # Job tổng hợp — TÊN `quality` LÀ BẤT BIẾN (required status check của branch protection `main`).
  # Thêm job con mới thì phải nối vào `needs` dưới đây, nếu không kết quả của nó không được tính.
  quality:
    name: quality
    runs-on: ubuntu-latest
    needs: [static, unit, eval-replay, studio-static, studio-unit, studio-eval-replay, gateway-static, gateway-unit]
    if: always()
    steps:
      - name: Kiểm mọi job con đã success
        run: |
          if [ "${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}" = "true" ]; then
            echo "Có job con không success" >&2
            exit 1
          fi
